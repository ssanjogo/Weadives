<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.weadives">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".PantallaPerfilAmigo.PantallaPerfilAmigo"
            android:exported="false" />
        <activity
            android:name=".PantallaMiPerfil.PantallaMiPerfil"
            android:exported="false" />
        <activity
            android:name=".PantallaGestorInundaciones.PantallaGestorInundaciones"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".

        <!

            -->
        <meta-data
             android:name="com.google.android.geo.API_KEY"
             android:value="${MAPS_API_KEY}"/>
        <activity
            android:name=".PantallaMapa.PantallaMapa"
            android:exported="false"
            android:label="@string/title_activity_pantalla_mapa" />
        <activity
            android:name=".PantallaInicio.PantallaInicio"
            android:exported="true"
            android:launchMode="singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PantallaPrincipal.PantallaPrincipal"
            android:exported="false" />
        <activity
            android:name=".PantallaLogIn.PantallaLogIn"
            android:exported="false" />
        <activity
            android:name=".PantallaRegistro.PantallaRegistro"
            android:exported="false" />
        <activity
            android:name=".AreaUsuario.AreaUsuario"
            android:exported="false" />
        <activity
            android:name=".AjustesPerfil.AjustesPerfil"
            android:exported="false" />
        <activity
            android:name=".ConfiguracionDePreferencias.ConfiguracionDePreferencias"
            android:exported="false" />
        <activity
            android:name=".PantallaDeHorarios.PantallaDeHorarios"
            android:exported="false" />
    </application>

</manifest>